(reference-concept-field)=

# Field

```{mermaid}
flowchart TD
    Field --> Raster
    Field --> Array
    Field --> Scalar
    Field --> Value
```

A `Field` is a property of an object which is defined everywhere, within the spatial bounds of the object.
