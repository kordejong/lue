#pragma once


#cmakedefine netCDF_HAS_PARALLEL


namespace lue::netcdf {

    class BuildOptions
    {

        public:

            /*!
                @brief      Whether or not the underlying NetCDF library supports parallel I/O
            */
            static constexpr bool netcdf_is_parallel{
#ifdef netCDF_HAS_PARALLEL
                    true
#else
                    false
#endif
                };

        };

} // namespace lue::netcdf
